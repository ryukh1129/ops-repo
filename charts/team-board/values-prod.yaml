# prod는 정확한 버전과 충분한 리소스
image:
  tag: "1.2.0"           # prod는 정확한 버전 명시

replicaCount: 3

ingress:
  host: app.example.com

resources:
  requests:
    cpu: 500m
    memory: 512Mi
  limits:
    cpu: 2000m
    memory: 2Gi

autoscaling:
  enabled: true
  minReplicas: 3
  maxReplicas: 20